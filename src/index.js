/////////////////////////////////////////////////////////////
// Importaciones

const { config } = require('dotenv');
const { ServerUp } = require("./app")


/////////////////////////////////////////////////////////////
// Función Principal

let main = () =>{


    //////////////////////////////////////////////////////////
    //Accedemos al .env

    config();

    /////////////////////////////////////////////////////////
    //Variables

    const PORT = process.env.PORT || 3000;

    /////////////////////////////////////////////////////////
    //Server

    ServerUp(PORT);

}


/////////////////////////////////////////////////////////////
// Ejecutamos Función Principal


main();